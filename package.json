{
  "name": "srcreigh.github.io",
  "private": true,
  "dependencies": {
    "metalsmith-templates": "~0.4.0",
    "metalsmith-markdown": "~0.2.1",
    "metalsmith-permalinks": "~0.3.0",
    "metalsmith": "~0.6.0",
    "metalsmith-less": "~0.1.1",
    "metalsmith-ignore": "~0.1.2",
    "jade": "~1.3.1"
  },
  "scripts": {
    "build": "nodejs ./build.js",
    "deploy": "npm run build && cd ./build && git init . && git add -A && git commit -m \"Deploy `date`\" && git push 'https://github.com/srcreigh/srcreigh.github.io.git' master --force && rm -rf .git && cd .."
  }
}
